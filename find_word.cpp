#include <iostream>
#include <list>
using namespace std;

bool is_contained(string word, string review) {
	if(review.find(word) != string::npos) {
		return true
	}
	else {
		return false
	}
}

int find_word(list<string> words_list, string reviews) {
	
}